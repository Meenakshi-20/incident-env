[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "incident-env"
version = "1.0.0"
description = "Incident Report Auto-Filler OpenEnv Environment"
requires-python = ">=3.11"
